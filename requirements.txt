# AI Engine dependencies
# Install with: pip install -r requirements.txt

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9       # required for FastAPI file uploads

# OCR
pytesseract>=0.3.10
Pillow>=10.3.0

# Semantic search
sentence-transformers>=2.7.0

# LLM service
httpx>=0.27.0
python-dotenv>=1.0.1

# Optional: if you use PyTorch-backed models (HuggingFace)
# torch>=2.2.0                # uncomment if not already installed
